# Catálogo de motivos + diagnóstico CIE-10 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Convertir el campo "Motivo" (control y remisión) en un autocompletado que va formando un catálogo, y el "Diagnóstico" de la remisión en un selector estricto contra la clasificación oficial CIE-10.

**Architecture:** Motivos es un catálogo de *sugerencias* desacoplado (tabla `motivos`, sin FK): controles/remisiones siguen guardando el texto del motivo, y un helper de upsert alimenta el catálogo al vuelo. CIE-10 es un catálogo de referencia (tabla `cie10` sembrada desde CSV) con relación opcional desde `remisiones.cie10_codigo`; la columna `diagnostico` de texto libre se conserva solo para leer registros viejos.

**Tech Stack:** NestJS 10 + Prisma 5 + PostgreSQL 16 (backend), React 18 + Vite 5 + Vitest + Tailwind (frontend), jsPDF (reportes).

## Global Constraints

- Idioma: es_CO para texto de usuario; inglés para código.
- Búsquedas de texto: siempre `mode: 'insensitive'` (Postgres `contains` es case-sensitive por defecto).
- Cambios de esquema: aditivos y preservadores de datos; nullable donde afecte filas existentes. Migración de producción transaccional con backup + verificación post-migración, mismo formato que `scripts/sql/migrations/2026-07-14-*.sql`.
- El backend **no** tiene framework de tests (jest deshabilitado): las tareas de backend se verifican con `curl` contra el dev server, como en features previas. El frontend usa Vitest (TDD real).
- Prisma serializa `Decimal` como número (parche ya aplicado en `prisma.service.ts`); no tocar.
- Rutas privadas por defecto; roles con `@Roles(...)`. Lectura: `ADMINISTRADOR, ENFERMERA, CONSULTA`.
- Commits: `feat|fix|docs(scope): descripción` en es. Commits frecuentes, uno por tarea como mínimo.
- Dev DB: Postgres en Docker `enfermeria-db`, `localhost:5435`, `DATABASE_URL` ya en `backend/.env`. Login dev: `admin`/`admin`.
- Token dev para curl:
  ```bash
  TOKEN=$(curl -s -X POST http://localhost:3001/api/auth/login -H 'Content-Type: application/json' -d '{"usuario":"admin","password":"admin"}' | sed -E 's/.*"token":"([^"]+)".*/\1/')
  ```

---

### Task 1: Dataset CIE-10 normalizado

**Files:**
- Create: `scripts/sql/data/cie10.csv` (encabezado `codigo,descripcion`, RFC4180)
- Create: `scripts/sql/data/README.md` (fuente, fecha de descarga, licencia, conteo)
- Create (temporal, no commitear): script de normalización en scratchpad

**Interfaces:**
- Produces: `scripts/sql/data/cie10.csv` — dos columnas `codigo` (VARCHAR ≤10, sin comas), `descripcion` (texto, con comillas dobles RFC4180 si contiene comas). Consumido por Task 2 (seed), Task 3 (SQL).

**Contexto de fuente:** Omar provee el export oficial de la tabla de referencia CIE-10 de SISPRO/MinSalud (~12.000 códigos), en formato **tab-delimitado** (TSV) con encabezado. Columnas del export:

```
Tabla  Codigo  Nombre  Descripcion  Habilitado  Aplicacion  IsStandardGEL  IsStandardMSPS  Extra_I:AplicaASexo  ...  Fecha_Actualizacion  IsPublicPrivate
```

Fila de ejemplo:
```
CIE10  A000  COLERA DEBIDO A VIBRIO CHOLERAE 01, BIOTIPO CHOLERAE  COLERA  SI  ...
```

Mapeo al catálogo:
- `codigo` ← columna **`Codigo`** (formato SISPRO sin punto, p. ej. `A000`; coincide con RIPS).
- `descripcion` ← columna **`Nombre`** (la descripción diagnóstica específica; **no** `Descripcion`, que es la etiqueta de categoría).
- Filtrar filas con `Habilitado = 'SI'` y `Tabla = 'CIE10'`.

Si el archivo llega como `.xlsx` (binario), guardarlo antes desde Excel como **"Texto (delimitado por tabulaciones)"** o "CSV UTF-8". El resultado normalizado tiene exactamente las columnas `codigo,descripcion`.

- [ ] **Step 1: Normalizar el export oficial a `codigo,descripcion`**

Escribir el normalizador como archivo en el scratchpad (no commitear) y ejecutarlo sobre el TSV oficial. El export usa tabuladores como separador y su columna `Nombre` puede contener comas (por eso la salida se genera como CSV RFC4180 con comillas):

Crear `<scratchpad>/normalize-cie10.js`:

```javascript
const fs = require('node:fs');

// El export oficial de SISPRO es TAB-delimitado; sus campos no contienen tabs.
function parseTsv(text) {
  return text
    .split(/\r?\n/)
    .filter((line) => line.length > 0)
    .map((line) => line.split('\t'));
}

const esc = (s) => (/[",\n]/.test(s) ? '"' + s.replace(/"/g, '""') + '"' : s);

const rows = parseTsv(fs.readFileSync(process.argv[2], 'utf8'));
const head = rows[0].map((h) => h.trim());
const iTabla = head.indexOf('Tabla');
const iCodigo = head.indexOf('Codigo');
const iNombre = head.indexOf('Nombre');
const iHabilitado = head.indexOf('Habilitado');
if (iCodigo < 0 || iNombre < 0) throw new Error('No se encontraron columnas Codigo/Nombre: ' + head.join('|'));

const out = ['codigo,descripcion'];
const seen = new Set();
for (const r of rows.slice(1)) {
  if (iTabla >= 0 && (r[iTabla] || '').trim() !== 'CIE10') continue;
  if (iHabilitado >= 0 && (r[iHabilitado] || '').trim().toUpperCase() !== 'SI') continue;
  const cod = (r[iCodigo] || '').trim();
  const desc = (r[iNombre] || '').trim().slice(0, 255);
  if (!cod || !desc || cod.length > 10 || seen.has(cod)) continue;
  seen.add(cod);
  out.push(esc(cod) + ',' + esc(desc));
}
fs.writeFileSync('cie10.csv', out.join('\n') + '\n');
console.log('filas:', out.length - 1);
```

Ejecutar (ruta al export que entrega Omar; si es `.xlsx`, guardarlo antes como texto tab-delimitado):

```bash
node <scratchpad>/normalize-cie10.js "<ruta-al-export-CIE10.txt>"
```

Copiar el `cie10.csv` resultante a `scripts/sql/data/cie10.csv`.

- [ ] **Step 2: Validar el dataset**

```bash
# Encabezado correcto, sin códigos vacíos, longitud de código ≤ 10, sin duplicados
node -e '
const fs=require("fs");
const lines=fs.readFileSync("scripts/sql/data/cie10.csv","utf8").trim().split(/\r?\n/);
if(lines[0]!=="codigo,descripcion"){throw new Error("encabezado inesperado: "+lines[0]);}
const codes=new Set();
for(const l of lines.slice(1)){const cod=l.split(/,(.+)/)[0]; if(!cod||cod.length>10) throw new Error("codigo invalido: "+l); if(codes.has(cod)) throw new Error("duplicado: "+cod); codes.add(cod);}
console.log("OK filas:", codes.size);
'
```
Expected: `OK filas: N` con N ≈ 12.000 (export oficial de SISPRO, ya filtrado a `Habilitado='SI'`). Si N < 8000, revisar el parseo/filtros.

- [ ] **Step 3: Documentar la fuente**

Crear `scripts/sql/data/README.md` con: fuente (export oficial de la tabla de referencia CIE-10 de SISPRO/MinSalud), fecha del export (`Fecha_Actualizacion` del archivo) y de la normalización (2026-07-16), mapeo aplicado (`Codigo`→`codigo`, `Nombre`→`descripcion`, filtro `Habilitado='SI'`), y el conteo de filas. Una nota: "regenerar con el script de normalización del scratchpad si se actualiza el export".

- [ ] **Step 4: Commit**

```bash
git add scripts/sql/data/cie10.csv scripts/sql/data/README.md
git commit -m "feat(cie10): dataset CIE-10 normalizado (codigo,descripcion) + fuente documentada"
```

---

### Task 2: Esquema Prisma (motivos, cie10, remisiones.cie10Codigo) + seed dev

**Files:**
- Modify: `backend/prisma/schema.prisma` (modelo `Remision` ~226-247; agregar modelos `Motivo` y `Cie10`)
- Modify: `backend/prisma/seed.ts` (agregar carga de `cie10` desde CSV + motivos de ejemplo)

**Interfaces:**
- Produces: modelos Prisma `Motivo { id, nombre @unique, createdAt }`, `Cie10 { codigo @id, descripcion, remisiones }`, y en `Remision`: `cie10Codigo String?`, relación `cie10 Cie10?`. Consumido por Tasks 4, 5, 6.

- [ ] **Step 1: Agregar los modelos y la columna al schema**

En `backend/prisma/schema.prisma`, agregar al final (antes o después de otros modelos):

```prisma
model Motivo {
  id        Int      @id @default(autoincrement())
  nombre    String   @unique @db.VarChar(255)
  createdAt DateTime @default(now()) @map("created_at")

  @@map("motivos")
}

model Cie10 {
  codigo      String     @id @db.VarChar(10)
  descripcion String     @db.VarChar(255)
  remisiones  Remision[]

  @@map("cie10")
}
```

Y dentro de `model Remision`, agregar (junto a `diagnostico`):

```prisma
  cie10Codigo    String?        @map("cie10_codigo") @db.VarChar(10)
  cie10          Cie10?         @relation(fields: [cie10Codigo], references: [codigo], onDelete: Restrict)
```

Y un índice dentro del bloque `Remision` (junto a los `@@index` existentes):

```prisma
  @@index([cie10Codigo], map: "idx_remisiones_cie10")
```

- [ ] **Step 2: Aplicar a la DB dev y regenerar el cliente**

Run:
```bash
cd backend && npx prisma db push && npx prisma generate
```
Expected: "Your database is now in sync with your Prisma schema" + "Generated Prisma Client". (Es aditivo; no pide `--accept-data-loss`.)

- [ ] **Step 3: Sembrar cie10 + motivos en el seed**

En `backend/prisma/seed.ts`, dentro de `main()` (antes del cierre), agregar la carga del CSV y unos motivos de ejemplo:

```typescript
  // CIE-10: cargar catálogo desde el CSV normalizado (idempotente)
  const fs = await import('node:fs');
  const path = await import('node:path');
  const csvPath = path.resolve(__dirname, '../../scripts/sql/data/cie10.csv');
  const parseCsv = (text: string): string[][] => {
    const rows: string[][] = [];
    let field = '', row: string[] = [], q = false;
    for (let i = 0; i < text.length; i++) {
      const c = text[i];
      if (q) {
        if (c === '"') { if (text[i + 1] === '"') { field += '"'; i++; } else q = false; }
        else field += c;
      } else if (c === '"') q = true;
      else if (c === ',') { row.push(field); field = ''; }
      else if (c === '\n') { row.push(field); rows.push(row); row = []; field = ''; }
      else if (c !== '\r') field += c;
    }
    if (field || row.length) { row.push(field); rows.push(row); }
    return rows;
  };
  const cieRows = parseCsv(fs.readFileSync(csvPath, 'utf8')).slice(1)
    .filter((r) => r[0] && r[1])
    .map((r) => ({ codigo: r[0].trim(), descripcion: r[1].trim() }));
  const cie = await prisma.cie10.createMany({ data: cieRows, skipDuplicates: true });
  console.log('CIE-10 sembrados:', cie.count);

  // Motivos de ejemplo (el catálogo real crece con el uso)
  const motivosDemo = ['Control de rutina', 'Dolor lumbar', 'Cefalea', 'Control de presión arterial'];
  for (const nombre of motivosDemo) {
    await prisma.motivo.upsert({ where: { nombre }, update: {}, create: { nombre } });
  }
  console.log('Motivos demo sembrados:', motivosDemo.length);
```

- [ ] **Step 4: Ejecutar el seed y verificar conteos**

Run:
```bash
cd backend && npm run prisma:seed
```
Expected: imprime `CIE-10 sembrados: N` (N > 2000) y `Motivos demo sembrados: 4`.

Verificar en la DB:
```bash
docker exec enfermeria-db psql -U enfermeria_user -d enfermeria_db -t -c "SELECT count(*) FROM cie10; SELECT count(*) FROM motivos;"
```
Expected: dos números; cie10 = N, motivos = 4.

- [ ] **Step 5: Commit**

```bash
git add backend/prisma/schema.prisma backend/prisma/seed.ts
git commit -m "feat(db): tablas motivos y cie10 + remisiones.cie10_codigo; seed carga CIE-10"
```

---

### Task 3: SQL de instalación limpia + migración de producción

**Files:**
- Modify: `scripts/sql/01-schema.sql` (agregar `CREATE TABLE motivos`, `CREATE TABLE cie10`, columna+FK en `remisiones`; nota de carga CSV)
- Create: `scripts/sql/migrations/2026-07-16-motivos-cie10.sql`

**Interfaces:**
- Consumes: `scripts/sql/data/cie10.csv` (Task 1).
- Produces: scripts SQL verificados contra DB scratch.

- [ ] **Step 1: Instalación limpia**

En `scripts/sql/01-schema.sql`, agregar tras el bloque de `CREATE TABLE "remisiones"`:

```sql
-- Catálogo de motivos (sugerencias; sin FK, crece con el uso)
CREATE TABLE "motivos" (
  "id" SERIAL NOT NULL,
  "nombre" VARCHAR(255) NOT NULL,
  "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "motivos_pkey" PRIMARY KEY ("id")
);
CREATE UNIQUE INDEX "motivos_nombre_key" ON "motivos"("nombre");

-- Catálogo CIE-10 (referencia oficial; cargar desde scripts/sql/data/cie10.csv)
CREATE TABLE "cie10" (
  "codigo" VARCHAR(10) NOT NULL,
  "descripcion" VARCHAR(255) NOT NULL,
  CONSTRAINT "cie10_pkey" PRIMARY KEY ("codigo")
);
```

Y dentro del `CREATE TABLE "remisiones"`, agregar la columna `"cie10_codigo" VARCHAR(10),` (tras `"diagnostico" TEXT,`), y después del `CREATE TABLE` agregar la FK + índice:

```sql
ALTER TABLE "remisiones"
  ADD CONSTRAINT "remisiones_cie10_codigo_fkey"
  FOREIGN KEY ("cie10_codigo") REFERENCES "cie10"("codigo")
  ON DELETE RESTRICT ON UPDATE CASCADE;
CREATE INDEX "idx_remisiones_cie10" ON "remisiones"("cie10_codigo");
```

Agregar un comentario indicando cargar los datos:
```sql
-- Cargar el catálogo CIE-10 tras crear las tablas:
--   \copy "cie10" FROM 'scripts/sql/data/cie10.csv' WITH (FORMAT csv, HEADER true)
```

- [ ] **Step 2: Migración de producción**

Crear `scripts/sql/migrations/2026-07-16-motivos-cie10.sql`:

```sql
-- Migración: catálogo de motivos + catálogo CIE-10 + remisiones.cie10_codigo
-- Fecha: 2026-07-16
-- Contexto: cambio ADITIVO. No toca datos existentes: las remisiones actuales
--   conservan su "diagnostico" de texto libre y quedan con cie10_codigo NULL.
--   El catálogo de motivos arranca vacío (crece con el uso).
--
-- ANTES DE EJECUTAR:
--   1. Backup:  pg_dump -U <user> -d <db> -F c -f backup_pre_motivos_cie10.dump
--   2. Puede correr con la app en línea (solo agrega tablas/columna nullable),
--      pero se recomienda junto con el despliegue del backend nuevo.
--
-- Ejecución:  psql -U <user> -d <db> -f 2026-07-16-motivos-cie10.sql
--   Luego cargar el catálogo CIE-10 (fuera de la transacción, requiere el CSV):
--     \copy "cie10" FROM 'scripts/sql/data/cie10.csv' WITH (FORMAT csv, HEADER true)

BEGIN;

CREATE TABLE "motivos" (
  "id" SERIAL NOT NULL,
  "nombre" VARCHAR(255) NOT NULL,
  "created_at" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "motivos_pkey" PRIMARY KEY ("id")
);
CREATE UNIQUE INDEX "motivos_nombre_key" ON "motivos"("nombre");

CREATE TABLE "cie10" (
  "codigo" VARCHAR(10) NOT NULL,
  "descripcion" VARCHAR(255) NOT NULL,
  CONSTRAINT "cie10_pkey" PRIMARY KEY ("codigo")
);

ALTER TABLE "remisiones" ADD COLUMN "cie10_codigo" VARCHAR(10);
ALTER TABLE "remisiones"
  ADD CONSTRAINT "remisiones_cie10_codigo_fkey"
  FOREIGN KEY ("cie10_codigo") REFERENCES "cie10"("codigo")
  ON DELETE RESTRICT ON UPDATE CASCADE;
CREATE INDEX "idx_remisiones_cie10" ON "remisiones"("cie10_codigo");

COMMIT;

-- Cargar el catálogo (tras el COMMIT):
--   \copy "cie10" FROM 'scripts/sql/data/cie10.csv' WITH (FORMAT csv, HEADER true)

-- Verificación post-migración:
--   \d motivos      -- id, nombre (único), created_at
--   \d cie10        -- codigo (PK), descripcion
--   \d remisiones   -- cie10_codigo VARCHAR(10) nullable + FK
--   SELECT count(*) FROM cie10;                                  -- = filas del CSV
--   SELECT count(*) FROM remisiones WHERE cie10_codigo IS NOT NULL;  -- 0 tras migrar
```

- [ ] **Step 3: Verificar la migración contra DB scratch**

Reconstruir el esquema PRE-cambio desde git, con una remisión de datos, y aplicar la migración:

```bash
# Esquema previo (HEAD anterior a esta rama) + una fila de remisión, luego migrar
docker exec enfermeria-db psql -U enfermeria_user -d postgres -c "DROP DATABASE IF EXISTS migration_check;" -c "CREATE DATABASE migration_check;"
git show HEAD:scripts/sql/01-schema.sql | docker exec -i enfermeria-db psql -U enfermeria_user -d migration_check
docker exec enfermeria-db psql -U enfermeria_user -d migration_check -c "INSERT INTO pacientes (nombre, apellido, numero_documento, tipo_documento, activo, created_at, updated_at) VALUES ('T','Est','X1','CC',true,now(),now());"
# (ajustar el INSERT de paciente/enfermera/remisión a las columnas NOT NULL reales del esquema previo)
cat scripts/sql/migrations/2026-07-16-motivos-cie10.sql | docker exec -i enfermeria-db psql -U enfermeria_user -d migration_check
docker exec enfermeria-db psql -U enfermeria_user -d migration_check -c "\d remisiones" -c "SELECT count(*) FROM remisiones WHERE cie10_codigo IS NOT NULL;"
```
Expected: la migración corre sin error; `\d remisiones` muestra `cie10_codigo` nullable con FK; el conteo es 0 y la remisión de prueba sigue existiendo. Luego `DROP DATABASE migration_check`.

> Nota: el `git show HEAD:` de arriba usa el esquema actual (que aún no tiene las tablas); tras Task 1 este script vive en la rama pero `01-schema.sql` ya se modificó en Step 1. Para el scratch usar la versión de `01-schema.sql` **anterior** a Step 1 (por ejemplo `git show <commit-antes-de-task3>:scripts/sql/01-schema.sql`), de modo que la migración tenga algo que agregar.

- [ ] **Step 4: Commit**

```bash
git add scripts/sql/01-schema.sql scripts/sql/migrations/2026-07-16-motivos-cie10.sql
git commit -m "feat(sql): motivos y cie10 en instalacion limpia + migracion de produccion"
```

---

### Task 4: Backend — módulo `cie10` (GET /api/cie10)

**Files:**
- Create: `backend/src/cie10/cie10.module.ts`, `backend/src/cie10/cie10.controller.ts`, `backend/src/cie10/cie10.service.ts`
- Modify: `backend/src/app.module.ts` (registrar `Cie10Module`)

**Interfaces:**
- Consumes: modelo Prisma `Cie10` (Task 2).
- Produces: `GET /api/cie10?q=` → `{ data: Cie10[] }` (hasta 20; busca por código o descripción, insensible). Consumido por el frontend (Task 7/9).

- [ ] **Step 1: Servicio**

Crear `backend/src/cie10/cie10.service.ts`:

```typescript
import { Injectable } from '@nestjs/common';
import { PrismaService } from '../prisma/prisma.service';

@Injectable()
export class Cie10Service {
  constructor(private prisma: PrismaService) {}

  async search(q?: string) {
    const where = q
      ? {
          OR: [
            { codigo: { contains: q, mode: 'insensitive' as const } },
            { descripcion: { contains: q, mode: 'insensitive' as const } },
          ],
        }
      : {};
    return this.prisma.cie10.findMany({
      where,
      orderBy: { codigo: 'asc' },
      take: 20,
    });
  }
}
```

- [ ] **Step 2: Controlador**

Crear `backend/src/cie10/cie10.controller.ts`:

```typescript
import { Controller, Get, Query, UseGuards } from '@nestjs/common';
import { ApiTags, ApiOperation, ApiBearerAuth } from '@nestjs/swagger';
import { Cie10Service } from './cie10.service';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { RolesGuard } from '../common/guards/roles.guard';
import { Roles } from '../common/decorators/roles.decorator';
import { Role } from '../common/enums/role.enum';

@ApiTags('CIE-10')
@ApiBearerAuth()
@UseGuards(JwtAuthGuard, RolesGuard)
@Controller('cie10')
export class Cie10Controller {
  constructor(private readonly cie10Service: Cie10Service) {}

  @Get()
  @Roles(Role.ADMINISTRADOR, Role.ENFERMERA, Role.CONSULTA)
  @ApiOperation({ summary: 'Buscar códigos CIE-10 por código o descripción' })
  search(@Query('q') q?: string) {
    return this.cie10Service.search(q);
  }
}
```

- [ ] **Step 3: Módulo + registro**

Crear `backend/src/cie10/cie10.module.ts`:

```typescript
import { Module } from '@nestjs/common';
import { Cie10Service } from './cie10.service';
import { Cie10Controller } from './cie10.controller';

@Module({
  controllers: [Cie10Controller],
  providers: [Cie10Service],
  exports: [Cie10Service],
})
export class Cie10Module {}
```

En `backend/src/app.module.ts`: importar `Cie10Module` y agregarlo al array `imports` (junto a los otros módulos).

- [ ] **Step 4: Build + verificar por curl**

Run:
```bash
cd backend && npm run build && npm run start:dev &
# esperar a que levante en :3001
```
Luego (con `$TOKEN` del bloque de Global Constraints):
```bash
curl -s "http://localhost:3001/api/cie10?q=diarrea" -H "Authorization: Bearer $TOKEN"
curl -s "http://localhost:3001/api/cie10?q=A09" -H "Authorization: Bearer $TOKEN"
```
Expected: JSON con hasta 20 resultados `{codigo, descripcion}` relevantes; la búsqueda por texto y por código funcionan (insensible a mayúsculas).

- [ ] **Step 5: Commit**

```bash
git add backend/src/cie10 backend/src/app.module.ts
git commit -m "feat(cie10): endpoint GET /api/cie10 (busqueda por codigo o descripcion)"
```

---

### Task 5: Backend — módulo `motivos` + `upsertMotivo` en controles/remisiones

**Files:**
- Create: `backend/src/motivos/motivos.module.ts`, `backend/src/motivos/motivos.controller.ts`, `backend/src/motivos/motivos.service.ts`
- Modify: `backend/src/app.module.ts` (registrar `MotivosModule`)
- Modify: `backend/src/controles/controles.module.ts`, `backend/src/controles/controles.service.ts`
- Modify: `backend/src/remisiones/remisiones.module.ts`, `backend/src/remisiones/remisiones.service.ts`

**Interfaces:**
- Consumes: modelo Prisma `Motivo` (Task 2).
- Produces:
  - `GET /api/motivos?q=` → `string[]` (hasta 20 nombres, alfabético, insensible).
  - `MotivosService.upsert(nombre: string): Promise<void>` — normaliza (trim + colapsa espacios) y upsert case-insensitive; best-effort (nunca lanza). Consumido por controles/remisiones.

- [ ] **Step 1: Servicio**

Crear `backend/src/motivos/motivos.service.ts`:

```typescript
import { Injectable, Logger } from '@nestjs/common';
import { PrismaService } from '../prisma/prisma.service';

@Injectable()
export class MotivosService {
  private readonly logger = new Logger(MotivosService.name);
  constructor(private prisma: PrismaService) {}

  async search(q?: string): Promise<string[]> {
    const where = q ? { nombre: { contains: q, mode: 'insensitive' as const } } : {};
    const rows = await this.prisma.motivo.findMany({
      where,
      orderBy: { nombre: 'asc' },
      take: 20,
      select: { nombre: true },
    });
    return rows.map((r) => r.nombre);
  }

  // Alta al vuelo: normaliza y registra la variante en el catálogo.
  // Best-effort: si falla, no debe romper el guardado del control/remisión.
  async upsert(nombre?: string | null): Promise<void> {
    const normalizado = (nombre ?? '').trim().replace(/\s+/g, ' ');
    if (!normalizado) return;
    try {
      const existente = await this.prisma.motivo.findFirst({
        where: { nombre: { equals: normalizado, mode: 'insensitive' } },
        select: { id: true },
      });
      if (!existente) {
        await this.prisma.motivo.create({ data: { nombre: normalizado } });
      }
    } catch (e) {
      this.logger.warn(`No se pudo registrar el motivo "${normalizado}": ${e}`);
    }
  }
}
```

- [ ] **Step 2: Controlador + módulo**

Crear `backend/src/motivos/motivos.controller.ts`:

```typescript
import { Controller, Get, Query, UseGuards } from '@nestjs/common';
import { ApiTags, ApiOperation, ApiBearerAuth } from '@nestjs/swagger';
import { MotivosService } from './motivos.service';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
import { RolesGuard } from '../common/guards/roles.guard';
import { Roles } from '../common/decorators/roles.decorator';
import { Role } from '../common/enums/role.enum';

@ApiTags('Motivos')
@ApiBearerAuth()
@UseGuards(JwtAuthGuard, RolesGuard)
@Controller('motivos')
export class MotivosController {
  constructor(private readonly motivosService: MotivosService) {}

  @Get()
  @Roles(Role.ADMINISTRADOR, Role.ENFERMERA, Role.CONSULTA)
  @ApiOperation({ summary: 'Buscar motivos del catálogo (autocompletado)' })
  search(@Query('q') q?: string) {
    return this.motivosService.search(q);
  }
}
```

Crear `backend/src/motivos/motivos.module.ts`:

```typescript
import { Module } from '@nestjs/common';
import { MotivosService } from './motivos.service';
import { MotivosController } from './motivos.controller';

@Module({
  controllers: [MotivosController],
  providers: [MotivosService],
  exports: [MotivosService],
})
export class MotivosModule {}
```

En `backend/src/app.module.ts`: importar y registrar `MotivosModule`.

- [ ] **Step 3: Inyectar `upsertMotivo` en controles**

En `backend/src/controles/controles.module.ts`, importar `MotivosModule` y agregarlo a `imports`.

En `backend/src/controles/controles.service.ts`:
- Inyectar `MotivosService` en el constructor: `constructor(private prisma: PrismaService, private motivos: MotivosService) {}` (agregar el import).
- En `create(...)`, tras el `this.prisma.control.create({...})`, capturar el resultado en una variable, llamar `await this.motivos.upsert(dto.motivo);` y retornar el resultado.
- En `update(...)`, tras el `this.prisma.control.update({...})`, `await this.motivos.upsert(dto.motivo);` antes de retornar.

Ejemplo del `create` resultante:

```typescript
  async create(dto: CreateControlDto, enfermeraId: number) {
    const control = await this.prisma.control.create({
      data: {
        pacienteId: dto.pacienteId,
        enfermeraId,
        fecha: new Date(dto.fecha),
        tipo: dto.tipo as TipoControl,
        presionSistolica: dto.presionSistolica,
        presionDiastolica: dto.presionDiastolica,
        temperatura: dto.temperatura,
        pulso: dto.pulso,
        saturacionO2: dto.saturacionO2,
        peso: dto.peso,
        talla: dto.talla,
        motivo: dto.motivo,
        observaciones: dto.observaciones,
      },
      include: { paciente: true, enfermera: { select: safeUserSelect } },
    });
    await this.motivos.upsert(dto.motivo);
    return control;
  }
```

- [ ] **Step 4: Inyectar `upsertMotivo` en remisiones**

En `backend/src/remisiones/remisiones.module.ts`, importar `MotivosModule` y agregarlo a `imports`.

En `backend/src/remisiones/remisiones.service.ts`:
- Inyectar `MotivosService`: `constructor(private prisma: PrismaService, private motivos: MotivosService) {}` (agregar import).
- En `create(...)`, capturar el resultado, `await this.motivos.upsert(dto.motivo);` y retornarlo. (El `update` de remisión no acepta `motivo`, así que no se toca.)

- [ ] **Step 5: Build + verificar por curl**

Run: `cd backend && npm run build` (y reiniciar dev server si hace falta).
```bash
# Crear un control con un motivo nuevo, luego buscarlo en el catálogo
PID=$(curl -s "http://localhost:3001/api/pacientes?limit=1" -H "Authorization: Bearer $TOKEN" | sed -E 's/.*"id":([0-9]+).*/\1/')
curl -s -X POST http://localhost:3001/api/controles -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' \
  -d "{\"pacienteId\":$PID,\"fecha\":\"2026-07-16T09:00:00\",\"tipo\":\"RUTINARIO\",\"motivo\":\"Chequeo de prueba XYZ\"}" >/dev/null
curl -s "http://localhost:3001/api/motivos?q=chequeo" -H "Authorization: Bearer $TOKEN"
```
Expected: el último curl devuelve un array que incluye `"Chequeo de prueba XYZ"` (búsqueda insensible). Repetir el POST con el mismo motivo y confirmar que **no** se duplica (`GET /api/motivos?q=chequeo` sigue con una sola entrada).

- [ ] **Step 6: Commit**

```bash
git add backend/src/motivos backend/src/app.module.ts backend/src/controles backend/src/remisiones
git commit -m "feat(motivos): catalogo con GET /api/motivos + alta al vuelo en controles y remisiones"
```

---

### Task 6: Backend — remisiones aceptan `cie10Codigo`

**Files:**
- Modify: `backend/src/remisiones/dto/remision.dto.ts`
- Modify: `backend/src/remisiones/remisiones.service.ts` (create/update/findAll/findOne: validar + incluir relación cie10)

**Interfaces:**
- Consumes: modelo `Cie10`, relación `Remision.cie10` (Task 2).
- Produces: `POST/PUT /api/remisiones` aceptan `cie10Codigo?`; validan existencia (400 si no existe); respuestas incluyen `cie10 { codigo, descripcion }`. `diagnostico` deja de aceptarse como entrada.

- [ ] **Step 1: DTOs**

En `backend/src/remisiones/dto/remision.dto.ts`:
- Agregar el import: `import { IsNumber, IsString, IsNotEmpty, IsOptional, IsEnum, MaxLength } from 'class-validator';`
- En `CreateRemisionDto`: **eliminar** el campo `diagnostico?` y agregar en su lugar:

```typescript
  @ApiPropertyOptional({ example: 'A09' })
  @IsOptional()
  @Transform(toUndefinedIfEmpty)
  @IsString()
  @MaxLength(10)
  cie10Codigo?: string;
```

- En `UpdateRemisionDto`: **eliminar** el campo `diagnostico?` y agregar el mismo bloque `cie10Codigo?` de arriba.

- [ ] **Step 2: Servicio — validación + persistencia + include**

En `backend/src/remisiones/remisiones.service.ts`:
- Importar `BadRequestException` de `@nestjs/common`.
- Agregar un helper privado:

```typescript
  private async assertCie10Exists(codigo?: string) {
    if (!codigo) return;
    const found = await this.prisma.cie10.findUnique({ where: { codigo } });
    if (!found) {
      throw new BadRequestException(`El código CIE-10 "${codigo}" no existe`);
    }
  }
```

- En `create(...)`: al inicio, `await this.assertCie10Exists(dto.cie10Codigo);`. En el objeto `data`, reemplazar `diagnostico: dto.diagnostico,` por `cie10Codigo: dto.cie10Codigo,` (los nuevos registros no reciben `diagnostico`). En el `include`, agregar `cie10: true`.
- En `update(...)`: `await this.assertCie10Exists(dto.cie10Codigo);` tras el `findOne`. En el bloque de armado de `data`, quitar `if (dto.diagnostico) data.diagnostico = dto.diagnostico;` y agregar `if (dto.cie10Codigo) data.cie10Codigo = dto.cie10Codigo;`. Agregar `cie10: true` al `include`.
- En `findAll(...)` y `findOne(...)`: agregar `cie10: true` a cada `include`.

- [ ] **Step 3: Build + verificar por curl**

Run: `cd backend && npm run build` (+ reiniciar dev server).
```bash
# Código válido: 201 con relación cie10 incluida
CODE=$(curl -s "http://localhost:3001/api/cie10?q=A09" -H "Authorization: Bearer $TOKEN" | sed -E 's/.*"codigo":"([^"]+)".*/\1/')
curl -s -X POST http://localhost:3001/api/remisiones -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' \
  -d "{\"pacienteId\":$PID,\"tipo\":\"ESPECIALISTA\",\"destino\":\"Test\",\"motivo\":\"Prueba CIE\",\"fechaRemision\":\"2026-07-16\",\"cie10Codigo\":\"$CODE\"}"
# Código inexistente: 400
curl -s -o /dev/null -w "%{http_code}\n" -X POST http://localhost:3001/api/remisiones -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' \
  -d "{\"pacienteId\":$PID,\"tipo\":\"ESPECIALISTA\",\"destino\":\"Test\",\"motivo\":\"Prueba CIE\",\"fechaRemision\":\"2026-07-16\",\"cie10Codigo\":\"ZZZ999\"}"
```
Expected: el primero devuelve la remisión con `"cie10":{"codigo":...,"descripcion":...}`; el segundo imprime `400`.

- [ ] **Step 4: Commit**

```bash
git add backend/src/remisiones
git commit -m "feat(remisiones): diagnostico por codigo CIE-10 (cie10Codigo) con validacion de existencia"
```

---

### Task 7: Frontend — tipos, servicios API y helper `formatDiagnostico`

**Files:**
- Modify: `frontend/src/types/index.ts` (tipo `Cie10`; `Remision.cie10Codigo`, `Remision.cie10`)
- Create: `frontend/src/api/cie10.service.ts`, `frontend/src/api/motivos.service.ts`
- Create: `frontend/src/lib/diagnostico.ts`, `frontend/src/lib/diagnostico.test.ts`

**Interfaces:**
- Consumes: endpoints `GET /api/cie10`, `GET /api/motivos` (Tasks 4, 5).
- Produces:
  - `type Cie10 = { codigo: string; descripcion: string }`.
  - `cie10Service.search(q: string): Promise<Cie10[]>`.
  - `motivosService.search(q: string): Promise<string[]>`.
  - `formatDiagnostico(r: Pick<Remision,'cie10'|'diagnostico'>): string`.

- [ ] **Step 1: Test del helper (falla)**

Crear `frontend/src/lib/diagnostico.test.ts`:

```typescript
import { formatDiagnostico } from './diagnostico';

test('usa código y descripción cuando hay CIE-10', () => {
  expect(formatDiagnostico({ cie10: { codigo: 'A09', descripcion: 'Diarrea' } })).toBe('A09 — Diarrea');
});

test('cae al diagnóstico de texto libre cuando no hay CIE-10', () => {
  expect(formatDiagnostico({ diagnostico: 'Dolor lumbar' })).toBe('Dolor lumbar');
});

test('devuelve guion cuando no hay ninguno', () => {
  expect(formatDiagnostico({})).toBe('-');
});
```

- [ ] **Step 2: Ejecutar el test (verificar que falla)**

Run: `cd frontend && npx vitest run src/lib/diagnostico.test.ts`
Expected: FAIL — `formatDiagnostico` no existe.

- [ ] **Step 3: Tipos**

En `frontend/src/types/index.ts`:
- Agregar: `export type Cie10 = { codigo: string; descripcion: string };`
- En la interfaz `Remision` (junto a `diagnostico?: string;`): agregar `cie10Codigo?: string;` y `cie10?: Cie10;`.

- [ ] **Step 4: Helper**

Crear `frontend/src/lib/diagnostico.ts`:

```typescript
import type { Cie10 } from '../types';

export function formatDiagnostico(r: { cie10?: Cie10 | null; diagnostico?: string | null }): string {
  if (r.cie10) return `${r.cie10.codigo} — ${r.cie10.descripcion}`;
  if (r.diagnostico) return r.diagnostico;
  return '-';
}
```

- [ ] **Step 5: Ejecutar el test (verificar que pasa)**

Run: `cd frontend && npx vitest run src/lib/diagnostico.test.ts`
Expected: PASS (3 tests).

- [ ] **Step 6: Servicios API**

Crear `frontend/src/api/cie10.service.ts`:

```typescript
import api from './axios';
import type { Cie10 } from '../types';

export const cie10Service = {
  search: async (q: string): Promise<Cie10[]> => {
    const { data } = await api.get('/cie10', { params: { q } });
    return data.data;
  },
};
```

Crear `frontend/src/api/motivos.service.ts`:

```typescript
import api from './axios';

export const motivosService = {
  search: async (q: string): Promise<string[]> => {
    const { data } = await api.get('/motivos', { params: { q } });
    return data.data;
  },
};
```

> Nota: confirmar la forma de respuesta contra `frontend/src/api/remisiones.service.ts` — usa `data.data`. Si el interceptor de axios ya desenvuelve, ajustar a `data`. Verificar con un servicio existente antes de asumir.

- [ ] **Step 7: Commit**

```bash
git add frontend/src/types/index.ts frontend/src/api/cie10.service.ts frontend/src/api/motivos.service.ts frontend/src/lib/diagnostico.ts frontend/src/lib/diagnostico.test.ts
git commit -m "feat(frontend): tipos Cie10, servicios cie10/motivos y helper formatDiagnostico (TDD)"
```

---

### Task 8: Frontend — componente `SuggestInput` + motivo en ambos modales

**Files:**
- Create: `frontend/src/components/SuggestInput.tsx`, `frontend/src/components/SuggestInput.test.tsx`
- Modify: `frontend/src/components/patient/NuevoControlModal.tsx` (campo Motivo)
- Modify: `frontend/src/components/patient/NuevaRemisionModal.tsx` (campo Motivo)

**Interfaces:**
- Consumes: `motivosService.search` (Task 7).
- Produces: `SuggestInput` — input de texto libre con sugerencias de strings. Props: `value: string`, `onChange: (v: string) => void`, `fetcher: (q: string) => Promise<string[]>`, `placeholder?: string`, `label?: string`, `required?: boolean`, `className?: string`. El valor es SIEMPRE el texto tecleado; elegir una sugerencia solo rellena el texto.

- [ ] **Step 1: Test del componente (falla)**

Crear `frontend/src/components/SuggestInput.test.tsx`:

```tsx
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { SuggestInput } from './SuggestInput';

test('muestra sugerencias al enfocar y al elegir una emite su texto', async () => {
  const fetcher = async () => ['Dolor lumbar', 'Cefalea'];
  let current = '';
  const onChange = (v: string) => { current = v; };
  render(<SuggestInput value="" onChange={onChange} fetcher={fetcher} placeholder="Motivo" />);
  const input = screen.getByPlaceholderText('Motivo');
  fireEvent.focus(input);
  await waitFor(() => screen.getByText('Cefalea'));
  fireEvent.mouseDown(screen.getByText('Cefalea'));
  expect(current).toBe('Cefalea');
});

test('el texto libre que no está en la lista se conserva como valor', () => {
  let current = '';
  const onChange = (v: string) => { current = v; };
  render(<SuggestInput value="" onChange={onChange} fetcher={async () => []} placeholder="Motivo" />);
  fireEvent.change(screen.getByPlaceholderText('Motivo'), { target: { value: 'Algo nuevo' } });
  expect(current).toBe('Algo nuevo');
});
```

- [ ] **Step 2: Ejecutar el test (verificar que falla)**

Run: `cd frontend && npx vitest run src/components/SuggestInput.test.tsx`
Expected: FAIL — `SuggestInput` no existe.

- [ ] **Step 3: Implementar `SuggestInput`**

Crear `frontend/src/components/SuggestInput.tsx` (espeja los patrones de `SearchSelect`, pero el valor es el texto, no un objeto):

```tsx
import { useEffect, useId, useRef, useState } from 'react';
import type { KeyboardEvent } from 'react';
import { cn } from '../lib/cn';
import { Input } from '../ui/Input';

type Props = {
  value: string;
  onChange: (value: string) => void;
  fetcher: (query: string) => Promise<string[]>;
  label?: string;
  placeholder?: string;
  required?: boolean;
  className?: string;
};

export function SuggestInput({ value, onChange, fetcher, label, placeholder, required, className }: Props) {
  const [items, setItems] = useState<string[]>([]);
  const [open, setOpen] = useState(false);
  const [loading, setLoading] = useState(false);
  const [activeIndex, setActiveIndex] = useState(0);
  const blurTimer = useRef<ReturnType<typeof setTimeout> | undefined>(undefined);
  const instanceId = useId();
  const listboxId = `si-listbox-${instanceId}`;
  const getOptionId = (i: number) => `si-opt-${instanceId}-${i}`;

  useEffect(() => {
    if (!open) return;
    const q = value.trim();
    let ignore = false;
    setLoading(true);
    const timer = setTimeout(() => {
      fetcher(q)
        .then((r) => { if (!ignore) { setItems(r); setLoading(false); } })
        .catch(() => { if (!ignore) { setItems([]); setLoading(false); } });
    }, q ? 250 : 0);
    return () => { ignore = true; clearTimeout(timer); };
  }, [value, open, fetcher]);

  useEffect(() => { setActiveIndex(0); }, [items]);
  useEffect(() => () => clearTimeout(blurTimer.current), []);

  const choose = (s: string) => { onChange(s); setOpen(false); };

  const handleKeyDown = (e: KeyboardEvent<HTMLInputElement>) => {
    if (e.key === 'Escape') { setOpen(false); return; }
    if (!open || loading || items.length === 0) return;
    if (e.key === 'ArrowDown') { e.preventDefault(); setActiveIndex((i) => Math.min(i + 1, items.length - 1)); }
    else if (e.key === 'ArrowUp') { e.preventDefault(); setActiveIndex((i) => Math.max(i - 1, 0)); }
    else if (e.key === 'Enter') { e.preventDefault(); const s = items[activeIndex]; if (s) choose(s); }
  };

  return (
    <div className={cn('relative', className)}>
      <Input
        label={label}
        required={required}
        placeholder={placeholder}
        value={value}
        onChange={(e) => { onChange(e.target.value); setOpen(true); }}
        onFocus={() => setOpen(true)}
        onBlur={() => { blurTimer.current = setTimeout(() => setOpen(false), 150); }}
        onKeyDown={handleKeyDown}
        role="combobox"
        aria-expanded={open}
        aria-autocomplete="list"
        aria-controls={listboxId}
        aria-activedescendant={open && !loading && items.length > 0 ? getOptionId(activeIndex) : undefined}
      />
      {open && (
        <ul
          id={listboxId}
          role="listbox"
          className="absolute z-10 mt-1 max-h-60 w-full overflow-auto rounded-sm border border-border bg-surface shadow"
        >
          {loading && <li className="px-3 py-2 text-sm text-faint">Buscando…</li>}
          {!loading && items.length === 0 && <li className="px-3 py-2 text-sm text-faint">Sin sugerencias</li>}
          {!loading && items.map((s, index) => {
            const isActive = index === activeIndex;
            return (
              <li key={s}>
                <button
                  id={getOptionId(index)}
                  type="button"
                  role="option"
                  aria-selected={isActive}
                  onMouseDown={(e) => e.preventDefault()}
                  onClick={() => choose(s)}
                  onMouseEnter={() => setActiveIndex(index)}
                  className={cn(
                    'flex w-full items-center rounded-sm px-3 py-2 text-left text-sm transition-colors',
                    'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent',
                    isActive ? 'bg-accent-soft text-accent-strong' : 'text-text hover:bg-surface-2',
                  )}
                >
                  {s}
                </button>
              </li>
            );
          })}
        </ul>
      )}
    </div>
  );
}
```

> Nota: confirmar que `Input` (en `frontend/src/ui/Input`) acepta `label` y `required` como props (los modales ya usan `<Input label=... />`). Si no, envolver como en el resto del formulario.

- [ ] **Step 4: Ejecutar el test (verificar que pasa)**

Run: `cd frontend && npx vitest run src/components/SuggestInput.test.tsx`
Expected: PASS (2 tests).

- [ ] **Step 5: Usar `SuggestInput` para el Motivo en NuevoControlModal**

En `frontend/src/components/patient/NuevoControlModal.tsx`:
- Import: `import { SuggestInput } from '../SuggestInput';` y `import { motivosService } from '../../api/motivos.service';`
- Reemplazar la línea del Motivo (`<Input label="Motivo" .../>`, línea ~152) por:

```tsx
        <SuggestInput
          label="Motivo"
          value={motivo}
          onChange={setMotivo}
          fetcher={(q) => motivosService.search(q)}
          className="col-span-2"
        />
```

- [ ] **Step 6: Usar `SuggestInput` para el Motivo en NuevaRemisionModal**

En `frontend/src/components/patient/NuevaRemisionModal.tsx`:
- Import de `SuggestInput` y `motivosService`.
- Reemplazar el `<Textarea label="Motivo" ... required />` (líneas ~116-122) por:

```tsx
        <SuggestInput
          label="Motivo"
          value={motivo}
          onChange={setMotivo}
          fetcher={(q) => motivosService.search(q)}
          required
          className="col-span-2"
        />
```
(La validación `if (!motivo.trim())` en `handleSubmit` se mantiene.)

- [ ] **Step 7: Suite + build**

Run: `cd frontend && npx vitest run && npm run build`
Expected: toda la suite en verde; build sin errores de tipos.

- [ ] **Step 8: Commit**

```bash
git add frontend/src/components/SuggestInput.tsx frontend/src/components/SuggestInput.test.tsx frontend/src/components/patient/NuevoControlModal.tsx frontend/src/components/patient/NuevaRemisionModal.tsx
git commit -m "feat(frontend): SuggestInput (texto libre + sugerencias) para el motivo en ambos modales"
```

---

### Task 9: Frontend — diagnóstico CIE-10 en la remisión + render en el detalle

**Files:**
- Modify: `frontend/src/components/patient/NuevaRemisionModal.tsx` (campo Diagnóstico → SearchSelect CIE-10)
- Modify: `frontend/src/pages/PacienteDetailPage.tsx` (mostrar `formatDiagnostico` bajo el motivo en la tabla de remisiones)

**Interfaces:**
- Consumes: `SearchSelect<Cie10>`, `cie10Service.search`, `formatDiagnostico` (Task 7), tipo `Cie10`.
- Produces: la remisión envía `cie10Codigo`; el detalle muestra el diagnóstico (CIE-10 o texto libre viejo).

- [ ] **Step 1: Diagnóstico CIE-10 en el modal de remisión**

En `frontend/src/components/patient/NuevaRemisionModal.tsx`:
- Imports: `import { SearchSelect } from '../SearchSelect';`, `import { cie10Service } from '../../api/cie10.service';`, y `Cie10` desde `../../types`.
- Reemplazar el estado `const [diagnostico, setDiagnostico] = useState('');` por `const [cie10, setCie10] = useState<Cie10 | null>(null);`
- En `reset()`, reemplazar `setDiagnostico('');` por `setCie10(null);`
- En `handleSubmit`, reemplazar `if (diagnostico) dto.diagnostico = diagnostico;` por `if (cie10) dto.cie10Codigo = cie10.codigo;`
- Reemplazar el `<Textarea label="Diagnóstico" .../>` por:

```tsx
        <div className="col-span-2 flex flex-col gap-1">
          <span className="text-xs uppercase tracking-wide text-faint font-medium">Diagnóstico (CIE-10)</span>
          <SearchSelect<Cie10>
            value={cie10}
            onChange={setCie10}
            fetcher={(q) => cie10Service.search(q)}
            getLabel={(c) => `${c.codigo} — ${c.descripcion}`}
            getKey={(c) => c.codigo}
            placeholder="Buscar por código o descripción"
          />
        </div>
```

- [ ] **Step 2: Mostrar el diagnóstico en el detalle del paciente**

En `frontend/src/pages/PacienteDetailPage.tsx`:
- Import: `import { formatDiagnostico } from '../lib/diagnostico';`
- En la tabla de remisiones del tab (celda del motivo, ~línea 400), mostrar el diagnóstico como segunda línea muda dentro de la misma celda (sin columna nueva):

```tsx
                      <TD>
                        <div>{r.motivo}</div>
                        <div className="text-xs text-faint">{formatDiagnostico(r)}</div>
                      </TD>
```

- [ ] **Step 3: Suite + build**

Run: `cd frontend && npx vitest run && npm run build`
Expected: suite en verde (ajustar cualquier fixture de `Remision` en tests que ahora referencie `cie10`/`cie10Codigo` si el compilador o un test lo exige); build sin errores.

> Si algún test existente (`PacienteDetailPage.test.tsx`, `RemisionesPage.test.tsx`) rompe por el cambio de forma, actualizar el mock de la remisión para incluir `cie10`/`cie10Codigo` opcionales — no cambiar el comportamiento probado.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/components/patient/NuevaRemisionModal.tsx frontend/src/pages/PacienteDetailPage.tsx
git commit -m "feat(frontend): diagnostico CIE-10 en remision (SearchSelect) + render en el detalle"
```

---

### Task 10: Verificación end-to-end + review final

**Files:** ninguno nuevo (verificación + posibles fixes menores).

- [ ] **Step 1: Builds y suite completos**

```bash
cd backend && npm run build && cd ../frontend && npm run build && npx vitest run
```
Expected: ambos builds OK; toda la suite Vitest en verde.

- [ ] **Step 2: Flujo real (dev server + curl / UI)**

Con backend y frontend levantados (backend :3001, frontend :3000):
- Crear un control con un motivo nuevo → abrir el modal de remisión y confirmar que el motivo aparece como sugerencia.
- Crear una remisión eligiendo un diagnóstico CIE-10 (buscar por "A09" y por "diarrea") → 201 con `cie10` incluido.
- `POST /api/remisiones` con `cie10Codigo` inexistente → 400.
- Abrir el detalle del paciente → la remisión muestra `código — descripción`; una remisión vieja (con `diagnostico` de texto libre, si existe) sigue mostrando su texto.

Documentar los resultados (status codes + evidencia) en el reporte de la tarea.

- [ ] **Step 3: Review final whole-branch**

Usar `superpowers:requesting-code-review` sobre el rango de la rama (merge-base..HEAD). Atender findings con fix subagents si aplica.

- [ ] **Step 4: Cierre**

Actualizar el ledger (`.superpowers/sdd/progress.md`) y proceder con `superpowers:finishing-a-development-branch` (merge a main + push al fork + actualizar/crear PR hacia `vsierrajc/enfermeria`).

---

## Notas de implementación

- **Dataset CIE-10 (Task 1):** Omar entrega el export oficial de SISPRO/MinSalud (~12k, TSV con columnas `Tabla/Codigo/Nombre/Descripcion/Habilitado/...`). Se mapea `Codigo`→`codigo` y `Nombre`→`descripcion`, filtrando `Habilitado='SI'` y `Tabla='CIE10'`. El resto del plan es agnóstico al contenido del dataset.
- **Sin framework de tests en backend:** las verificaciones de backend son por `curl` contra el dev server. Si en el futuro se agrega jest/supertest, convertir estos curls en e2e.
- **`diagnostico` legado:** la columna se conserva; nunca se escribe en registros nuevos. No hay backfill (fuera de alcance).
- **Forma de respuesta axios:** confirmar `data.data` vs `data` contra un servicio existente antes de cerrar Task 7.
